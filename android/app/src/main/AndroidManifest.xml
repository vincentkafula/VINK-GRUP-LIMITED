<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->
    <!-- Each permission here maps to a real, existing feature in this app,
         not requested speculatively: -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required by the Deka EMV SDK (P18QTerminalPlugin), per the
         vendor's own Interface Document's project-setup section.
         READ_PHONE_STATE is a "dangerous" permission on Android 6.0+
         and this manifest entry alone does not grant it -- a runtime
         permission request (ActivityCompat.requestPermissions) is
         still needed before it takes effect, not yet implemented. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- RideHailingSystem's live driver tracking, DriveDashboardViewer's
         device location, PassengerDashboard's accessible-transport
         tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- So the WebSocket client (liveSocket.ts) can detect connectivity
         changes and reconnect promptly rather than relying only on its
         own timeout -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Live trip/tap/deadline notifications (Android 13+ requires this
         to be requested explicitly at runtime; declaring it here is the
         manifest-level prerequisite) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
